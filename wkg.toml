# WIT dependency manifest for wkg.
# Run `wkg wit fetch` from the repo root to populate wit/deps/.
# cargo-component will also fetch these automatically on first build.

[package]
name    = "radio:station"
version = "0.1.0"

[dependencies]
"wasi:http"   = "0.2.3"
"wasi:clocks" = "0.2.3"
"wasi:io"     = "0.2.3"
